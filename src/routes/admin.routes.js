import {Router} from 'express'

const router = Router();

router.get('/')
router.post('/')
router.post('/verify')
router.get('/users')
router.get('/chats')
router.get('/messages')
router.get('/stats')

// not used yet
export default router;
